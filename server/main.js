import { Meteor } from 'meteor/meteor';
import '../imports/api/vinos'
import '../imports/api/comidas'
import '../imports/api/usuarios'
import '../imports/api/pedidos'

Meteor.startup(() => {
  // code to run on server at startup
});
